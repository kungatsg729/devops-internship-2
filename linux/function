#!/bin/bash
echo "=============================="
echo " System Info — $(date)"
echo "=============================="
echo "User             : $(whoami)"
echo "Date             : $(date)"
echo "Current Directory: $(pwd)"
echo "Local IP         : $(ip route get 1.1.1.1 2>/dev/null | awk '{print $7}' | head -1)"
echo "Public IP        : $(curl -s --max-time 2 ifconfig.me || echo 'unavailable')"
echo ""
echo "Disk Usage:"
df -h /
echo ""
echo "Memory Usage:"
free -h
